# ── Presence Configuration ──────────────────────────
# Copy this file to .env and fill in your values.

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# WhatsApp — use your SECONDARY number, never your primary
WHATSAPP_TARGET_NUMBER=447XXXXXXXXX
# Format: country code + number, no + or spaces

# Timing
MORNING_PROMPT_TIME=06:30
# Skip days (comma-separated, lowercase)
SKIP_DAYS=sunday
# Timezone
TIMEZONE=Europe/London

# Paths
DB_PATH=./data/presence.db
AUTH_PATH=./data/auth

# Safety
DAILY_API_LIMIT=50
MONTHLY_SPEND_CAP=50
